// Firefox (and maybe other browsers) saves form data between refreshes.
// This means that buttons may retain disabled/enabled properties.
// I call this method to ensure a full reset.
updateDivButtons();
